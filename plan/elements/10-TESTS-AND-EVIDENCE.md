# 10 — Tests and evidence

Critical behaviour is proven through real workflows, not lines hit (AGENTS.md §5). Visual claims
get render-backed evidence (`product-discipline.mdc` §8). Heavy suites (e2e, the oracle, perf) run
in **CI**, whose results are read asynchronously for the latest head SHA and never waited on.
Targeted local tests run in the background while work continues, one heavy job at a time if at
all — local full runs have taken the 16 GB development Mac past its memory and shut it down.

---

## 1. Test matrix

| Layer               | What is tested                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Where                                                                                                                                                                                                                                    |
| ------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **timeline-schema** | `ShapeParamsSchema` (every key, range, strictness, colour forms); the v24→v25 (and later v26) passthrough migrations and round trips; newer-schema refusal; JSON-schema drift; shape-catalogue drift TS ↔ Python                                                                                                                                                                                                                                                                                                                                                                              | `shape-params.test.ts`, `migrations.test.ts`, `json-schema.test.ts`, `shape-catalog.test.ts`, `test_schema_parity.py`                                                                                                                    |
| **editor-core**     | `synthetic-assets.ts` + its **guard test** (no synthetic-id comparison outside the helper); `add_shape` apply + invert, validator rules (each rule, each message has a remedy, no varying magnitude), deterministic ids; `set_effect_params` re-validation on shapes; `addShapePatch` / `addStickerPatch` property tests (one patch, never overlaps a lane, undo deep-equal, a second add reuses the asset); `loop-motion.ts` builder (EL7); frame-plan vectors for stills (crop, opacity), titles (opacity, In/Out envelope), shape layers (bounds from params), animated frame index (EL10) | `synthetic-assets.test.ts`, `operations.test.ts`, `validator.test.ts`, `element-placement.test.ts`, `loop-motion.test.ts`, `operation-algebra.property.test.ts`, `cross-runtime-operation-behavior.test.ts`, `frame-plan.parity.test.ts` |
| **engine**          | stills and titles through the pipeline (the 00 §3 G1/G6/G11 cases as regression tests); own alpha × opacity; `shape_geometry` unit tests and `shape_raster` goldens (fixed sizes, byte checksums) — the engine is the only rasteriser, so no cross-runtime geometry fixture; `_compile_shape_clip`; the raster route (`kind: 'shape'`, validation, limits); render-validation checks; `synthetic_assets` guard test; `AnimatedImageClip` timing and memory bound (EL10); Pydantic parity                                                                                                      | `test_render_compiler.py`, `test_shape_geometry.py`, `test_shape_raster.py`, `test_service_preview_raster.py`, `test_render_validation.py`, `test_synthetic_assets.py`, `test_animated_image.py`                                         |
| **web-editor**      | `ElementsPanel` (tabs, remembered tab, first-open rule, browser absence); `PexelsBrowser` (every former Stock case); `ShapesBrowser` (EL4a six tiles, keyboard; EL5 chips, search, colour row); `StickersBrowser` (EL6a grid + search + tile states; EL6b virtualisation, chips, favourites); Inspector Shape / Sticker / Animation sections; on-canvas box and endpoint handles (one patch per gesture); timeline drop of `ELEMENT_DND_TYPE` (EL5); `coerceLeftTab` alias; Credits grouping; `toolMeta` labels                                                                               | `components/elements/*.test.tsx`, `Inspector.*.test.tsx`, `PreviewTransform.test.tsx`, `TimelineView.interactions.test.tsx`, `Editor.test.tsx`, `CreditsSection.test.tsx`                                                                |
| **desktop**         | `ElementsLibrary` (unknown id, dedupe, integrity, missing file, ENOSPC, concurrency, traversal-shaped ids; bundled vs packaged roots); IPC contract ↔ preload ↔ handler (`preload-channel-parity.test.ts`, `main-channel-registration.test.ts`); `sticker-host` (bin vs timeline, failure sentences); EL10 downloads (pin mismatch, size cap, cancel leaves no temp)                                                                                                                                                                                                                          | `media/elements-library.test.ts`, `ipc/*.test.ts`, `ai/sticker-host.test.ts`                                                                                                                                                             |
| **ai-sdk**          | tool schemas (strict; the shape-id enum; lenient aliases); the `add_sticker` orchestrator arm calls `addStickerPatch`, never the cutaway placer; **`add_clip` / `add_clips` / `move_clip` of an element asset delegate to the sticker builder** (the G3 trap, with a portrait project so `autoReframeCrop` would otherwise fire); agent-vs-panel deep-equal; domain routing phrases; critic checks; digest wording and size; catalogue search; skill description ≤ 300 chars; generated fixtures and goldens                                                                                  | `domain-tools/elements.test.ts`, `domain-tools/picture-layers.test.ts`, `orchestrator-stream.test.ts`, `tool-domains.test.ts`, `critic.test.ts`, `providers/elements/*.test.ts`                                                          |
| **mcp-server**      | the element tools exposed/hidden as 07 §7 says; cross-host parity                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | `tools.test.ts`, `cross-host-parity.test.ts`                                                                                                                                                                                             |
| **e2e**             | `elements.spec.ts` (browser build): the tab per build, no provider host in the page; `view-prefs-persist.spec.ts`: a stored `'stock'` opens Elements; `accessibility.spec.ts`: axe clean on the Elements panel in both themes. **Desktop flows** run in a CI project modelled on `masking-e2e` (the `masking/fake-desktop.ts` harness + a real sidecar): add a highlight box → resize on the canvas → recolour → export a frame → undo (EL4a); add a sticker → fade it → undo (EL6a); visual snapshots of each sub-tab                                                                        | `tests/e2e/specs/`                                                                                                                                                                                                                       |
| **PX4 oracle**      | every row in 05 §8, added passing                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | `preview-parity-oracle.spec.ts`                                                                                                                                                                                                          |
| **performance**     | 02 §9 and 05 §7 budgets: search keystroke, grid scroll, add latency, monitor with 20 elements, shape-raster time, export ratio                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `preview-scale-perf.spec.ts` (+ elements row), a grid perf spec, pytest benchmarks, the PX5.11-style dispatch workflow                                                                                                                   |
| **evaluation**      | one case per category inside its DoD (07 §8): EL4a case 1, EL6a case 2, EL7 case 3; EL8 the rest; hit rates reported over repeated runs                                                                                                                                                                                                                                                                                                                                                                                                                                                       | the golden set; recorded reports, never a local golden run                                                                                                                                                                               |
| **licences**        | every bundled library has its licence file beside it; `pnpm license:scan` on manifest changes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `elements-licences.test.ts`, CI                                                                                                                                                                                                          |

**Every new op tests apply and invert. Every new validator rule tests its message. No skipped tests
without a linked issue.**

---

## 2. What "done" looks like per category (the manual path, scripted in e2e where possible)

1. Open Elements → sub-tab → (search) → the right tiles appear.
2. Click a tile → a clip appears at the playhead on a graphics lane, selected, handles visible.
3. Move/resize/rotate on the canvas; change a style in the Inspector.
4. Scrub: the monitor shows it; `grab_frame` at the same time shows the same pixels (oracle row).
5. Export: the element is in the file (render validation passes).
6. Undo: the clip (and, if created by the add, the lane, folder and asset) is gone; redo restores.
7. Save, close, reopen: identical.
8. Failure: one forced failure per category shows its sentence (disk full, missing file, invalid
   shape params, no key for Photos).
9. The agent does the same through its tool, and the category's eval case is reported.

---

## 3. Parity oracle discipline

- New rows are added **passing**; the baseline JSON keeps no listed failures.
- Rows sample at transition/animation boundaries (start, mid, ±1 frame of the end) and at a loop
  wrap.
- A row that starts failing after a compositor or engine change fails the job — the same rule as
  PX4 today.

---

## 4. Desktop evidence runs (`product-discipline.mdc` §8)

Run on a desktop build against **real** media, reports committed under `docs/reports/elements/`:

| Run                             | Phase                                      | Material                                                   | What is measured                                                                                                                                                                           |
| ------------------------------- | ------------------------------------------ | ---------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **A — Callouts on a SaaS demo** | EL4a (shapes), extended in EL6a (stickers) | a real 5–15 min screen recording, 16:9                     | five callouts placed by hand and by the agent, timed to narration; export; monitor vs export frames at each element's midpoint; add latency; reopen; undo all                              |
| **B — Agent on the same demo**  | EL8                                        | the same recording                                         | eval cases 1, 4, 5, 6 (07 §8); timeline outcomes and rendered frames judged, hit rates reported                                                                                            |
| **C — Short-form talking head** | EL7                                        | real 9:16 talking-head clip                                | reaction stickers with In / Loop / Out; safe-area and face-overlap advisories; export at 1080×1920                                                                                         |
| **D — Scale**                   | EL6b                                       | 3-minute 4K timeline + 20 elements (3 animated after EL10) | PX5 monitor budgets on an M-series Mac; export ratio ≤ 1.3×                                                                                                                                |
| **E — Compatibility**           | EL2a, EL4a                                 | a v24 project with photos, titles and stock                | opens unchanged; renders the same except the listed G1/G6/G11 fixes; a v25 project refused by the previous release with `NEWER_SCHEMA_MESSAGE`; a stored `'stock'` rail tab opens Elements |

A claim of "done" for a category cites the run and the commit it ran on.

**Last updated:** 2026-09-26
