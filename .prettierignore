node_modules
dist
build
out
.turbo
coverage
.pytest_cache
reports
playwright-report
test-results
pnpm-lock.yaml
engine/python/.venv
**/__pycache__
*.fp.json
CHANGELOG.md

# Generated from the Zod source of truth via `schema:generate`; the generator
# (JSON.stringify) owns its formatting, and a TS test guards it against drift.
packages/timeline-schema/schema/project.schema.json

# Agent skill playbooks. Their YAML frontmatter is parsed line-by-line by
# `packages/ai-sdk/scripts/generate-skills.mjs`, which requires `tools: [...]` to be a
# SINGLE line — Prettier wraps a long list across lines and the generator (and therefore
# the build) then fails on it. The bodies are prose the generator embeds verbatim.
packages/ai-sdk/skills/*.md

# Written by generators that own their formatting and assert it byte for byte:
# `pnpm mask-raster:vectors`, `pnpm key-mask:vectors`, and the vitest file snapshot of the
# legacy mask migration.
tests/fixtures/mask-raster/*.json
tests/fixtures/mask-key/*.json
tests/fixtures/mask-render/legacy-v21.migrated.json

# Per-glyph title metrics measured from the bundled fonts by
# `python -m framepilot_engine.render.title_metrics`, which owns the formatting;
# engine/python/tests/test_title_metrics.py asserts the file byte for byte.
packages/ai-sdk/src/title-metrics.generated.ts

# Written by scripts/elements/build_library.py (one sticker per line) and
# scripts/elements/build_icons.mjs (one line of paths); the generators own their formatting
# and tests pin the files to the libraries they describe.
packages/ai-sdk/src/providers/elements/sticker-catalog.generated.ts
scripts/elements/fluent.lock.json
packages/timeline-schema/schema/shape-icons.json
packages/timeline-schema/src/shape-icon-names.ts
