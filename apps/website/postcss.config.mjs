/** Tailwind CSS v4 uses a single PostCSS plugin; no tailwind.config needed
 *  (theme is defined CSS-first via `@theme` in src/app/globals.css). */
const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};

export default config;
