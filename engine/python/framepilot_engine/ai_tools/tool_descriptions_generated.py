# ruff: noqa — generated file; long description strings trip line-length rules
"""GENERATED FILE — DO NOT EDIT. Source: ``packages/ai-sdk/src/tool-registry.ts``.

Tool descriptions the model reads, mirrored from the TS registry so the sidecar,
the MCP server and the desktop agent describe every tool in one voice.
Regenerate with ``pnpm --filter @framepilot/ai-sdk build``.
"""

from __future__ import annotations

TOOL_DESCRIPTIONS: dict[str, str] = {
    "add_asset": "Register a media file that ALREADY EXISTS on disk into the bin. Downloads and creates nothing: stock goes through add_stock and music through add_music, which fetch the file and supply its real path. A path you were not handed is refused. Does not place it on the timeline — use add_clip for that.",
    "add_caption_layer": "Add ONE short transcript-driven caption cue on a track over a timeline range (start/end seconds). To caption a whole recording use caption_the_edit instead — it segments and writes every cue in a single call. Reach for this one only to patch a specific gap by hand: it needs get_mapped_transcript first, and a range longer than one readable phrase (3–7 words, never more than 12) is rejected. Style the completed set track-wide.",
    "add_clip": "Place an existing asset on a track: start/end are timeline seconds; assetId must be copied from the project asset list and trackId must be copied from the timeline track list — these are separate id namespaces. sourceStart picks where playback begins in the asset (default 0), so the same asset can be reused many times by giving each placement a different sourceStart. sourceEnd is NOT yours to set: the host always derives it from the timeline span at 1x (sourceStart + end - start) and ignores any sourceEnd you send, because add_clip has no speed argument and the two must agree. To play a specific source range, set sourceStart and make the timeline span the same length. Read the timeline and assets first so you use real track/asset ids, and pick a track whose range is free — clips on one track can never overlap.",
    "add_clips": "Place MANY assets on one track in a single call — the same placement as add_clip, once per entry, in one reversible patch and one undo. Use this whenever you are laying out a sequence rather than fixing one shot: a montage, a b-roll pass, a set of photos on a beat grid. Entries are { assetId, start, end, sourceStart? } and follow add_clip’s rules exactly — timeline seconds, real asset ids, no overlaps on the track, and sourceEnd derived for you. The whole call is validated together, so if any one entry is rejected none of them land and the reason names the entry: fix that one and send the batch again. At most 100 entries per call — split a longer sequence across consecutive calls.",
    "add_keyframes": "Animate a clip property (e.g. scale, opacity, x, y) with keyframes; times are seconds from the clip's start. For a simple zoom, prefer punch_in.",
    "add_marker": "Add a marker (or named \"chapter\" point) at a position on the project timeline (schema v9). Give a time in seconds, an optional label to promote it to a chapter, and an optional CSS color for the scrub-bar. A deterministic id is derived from time+label when none is supplied.",
    "add_mask": "Add a mask shape to a clip.",
    "add_music": "Put a track from search_music under the edit. Pass its remoteId; it lands on its own music track, from atSeconds (default the start), the full length of the track. Give duckUnderTrackId the dialogue track id to have the bed drop under the voice — that is almost always what you want under narration. Downloads the file into the project, so it keeps working offline. If the track requires crediting, the project records the credit and says so. Undoing removes the track, its layer and the file reference in one step.",
    "add_stock": "Download a photo or clip from search_stock into this project. Pass its remoteId and kind. With atSeconds it also lands on the timeline as a cutaway at that moment; WITHOUT atSeconds it just arrives in the media bin, which is how you gather several candidates before deciding the order — place them later with add_clip. Fetched at the project’s own resolution, so it keeps working offline. A placement FAILS with a reason if that moment already has picture on it: stock cannot yet sit on top of existing footage, so choose an empty stretch or make room first. Undoing removes the clip and the file reference in one step.",
    "add_text_layer": "Add a text overlay clip on a track over a timeline range (start/end seconds). Clips on one track can never overlap — stack simultaneous text elements on separate tracks with a free range. Style it here: sizePercent is the glyph height as a percentage of the frame (8 is a caption, 18+ is a headline that dominates the frame), xPercent/yPercent place the box centre (50/50 is the middle, y 15 is a title card near the top), and color/background/align/boxWidthPercent do what they say. Everything renders exactly as the preview shows it. For motion, follow this with punch_in on the clip it creates.",
    "add_track": "Create a new empty track (a \"layer\") to get a free lane for clips that would otherwise overlap. Clips on one track can never overlap, so this is how you stack simultaneous elements — a title over b-roll, picture-in-picture, an extra overlay, or a second audio bed — when no existing track has a free range. `type` is the track's advisory role (video/audio/caption/overlay): it sets the default label/icon only, not a content limit, so any clip can live on any track. `atIndex` is the z-order slot where index 0 is the visual front (nearer the viewer); omit it to add the track in front. Pass `id` to name the track so you can reference it as trackId in the same turn (in add_clip/add_text_layer); otherwise a deterministic id is generated and appears in the next get_timeline.",
    "add_transition": "Add a transition at the cut between two adjacent clips on the same track. Requires trackId plus fromClipId and toClipId (the outgoing and incoming clip ids, which must be neighbours), a kind, and a positive durationSeconds. Read the timeline first to get the real track and clip ids; a transition needs two clips, so it cannot be added when the track has only one clip. Kinds come from the transition catalog — `cross-dissolve`, `whip-pan-left`, `glitch`, `circular-wipe` and 70-odd more; `discover_transitions` names them all. A cut can carry at most half of its shorter clip; ask for longer and the transition is shortened to fit rather than refused, so short clips take one too. Read the applied duration back before describing it to the editor.",
    "adjust_audio": "Adjust a clip’s audio gain (dB).",
    "adjust_effect": "Retune an applied effect. `params` is a PARTIAL patch — send only the values to change. `intensity` (0–1) is the master strength every effect honours; pass null to reset it to full. Call discover_effects for the valid parameter names and ranges of the effect’s kind.",
    "analyze_silence": "Detect silent ranges in an asset's audio (ffmpeg silencedetect). Returns start/end/duration for each gap; does not edit the timeline.",
    "apply_color_grade": "Grade ONE clip. Two kinds: `color_grade` (the default) takes signed offsets where 0 changes nothing — exposure (-5..5), contrast (-1..1), saturation (-1..3), temperature (-1..1), tint (-1..1), shadows (-1..1), highlights (-1..1). A value outside its range, or a name not on that list, is refused rather than silently ignored, and every parameter you omit stays at 0, so a correction can name only the axis it fixes. `lut` instead takes params.path — a .cube file inside the project. There is no grade for position, scale or rotation: those are keyframes (add_keyframes, punch_in). Grading is per clip, so a whole-sequence look is one call per clip.",
    "apply_effect": "Apply a catalog effect as its own timeline LAYER over a time range. The effect affects every visible clip beneath it for that range — it is not attached to one clip. Use discover_effects first to get a real effectId and its parameter ranges. Creates an effect track if the project has none. Omit endTime to use the effect’s own default duration.",
    "ask_user": "Ask the editor a question and wait for their answer. Use it when only they can settle something: the request is ambiguous, a capability you need does not exist so you must offer alternatives, you are about to do something large or hard to undo, or the answer depends on taste or on what they know about the footage. Write your own question and, when it is a choice, 2-5 concrete options (a short label each, plus a description when the consequence is not obvious); omit options to invite a free-text answer. Their answer comes back as the result and you continue from it. This tool is the ONLY way to ask: a question written into your reply text cannot be clicked or answered — this call renders your options as selectable choices and pauses until the editor picks. Ask instead of guessing when a wrong guess would waste their footage or their time — but never ask what a tool can tell you, and never ask twice about the same thing. Does not edit the timeline.",
    "auto_emphasize_captions": "Apply AI-selected semantic emphasis to a caption track as one reversible operation. First read get_mapped_transcript, reason about meaning, emotion, contrast, numbers, delivery and payoff, then submit 1-12 sparse exact spoken keywords. A keyword may be a phrase (\"stop scrolling\"), which emphasises the whole run of words that speaks it. The tool grounds every keyword against the captions/transcript and rejects invented text; a phrase must be spoken as consecutive words. Existing track styling is preserved; change the design itself with set_track_caption_style.",
    "caption_the_edit": "Caption the WHOLE edit in one call. Reads the mapped transcript, segments the retained speech into readable phrase cues at linguistic breaks, and writes every cue on the track — replacing whatever cues are already there. This is the tool to use for \"add captions\"; it is also how you REPAIR captions that verify_captions reports as stale or out of sync after a cut, because re-running it re-derives every cue from the current timeline. Words in deleted footage are dropped and no cue can span a cut, so it cannot produce the errors hand-placed cues do. preset picks the register: short-form (default, punchy 1-2 lines), subtitle (broadcast, longer reading lines), one-word (one word per cue, for the one-word template family). Style the finished track with set_track_caption_style and emphasise with auto_emphasize_captions. Requires a transcript — run transcribe first.",
    "delete_clip": "Delete one clip by id. Set ripple: true to also close the gap it leaves (later clips on its track shift earlier). Safer than delete_range/ripple_delete when you mean a specific clip — no hand-computed times.",
    "delete_clips": "Delete several clips by id in one call (max 50). Set ripple: true to close each gap (later clips shift earlier). Use for multi-cut edits like removing every flagged clip — one call instead of many delete_clip calls.",
    "delete_range": "Delete a timeline range (seconds) on one track, leaving a gap. Use ripple_delete instead when the gap should close.",
    "describe_footage": "Walk ONE asset in time order — its scene captions and spans from start to end — the \"what am I looking at\" primer before you plan cuts on it. Returns the same evidence packets as search_visual, sorted by time rather than ranked by a query, so you can read the footage as a sequence. Use search_visual instead when you are looking for a specific thing across all footage. Optional timeRange limits the walk. Honestly reports when the asset is not indexed yet. Does not edit the timeline.",
    "detect_beats": "Detect musical beat/onset timestamps in an asset's audio (energy-flux onset detection) plus an estimated BPM. Use for beat-synced montage cuts. Returns beat times in seconds; does not edit the timeline. Needs an asset that has an audio track — silent footage has no beats, so pass the music asset's id. Set hardSync when the editor asked for cuts ON the beat — \"cut to the beat\", \"beat-synced\", \"on every drop\" — because that is a promise about the rhythm and the runtime then holds you to it, rejecting a cut it cannot place on an onset. Leave it off — the default — when the music informs the rhythm but the picture leads, which is the more common case: near-misses are still snapped for you, and a cut deliberately off the grid is reported to you rather than refused.",
    "detect_scenes": "Detect scene-cut timestamps in an asset's video (ffmpeg scene score). Returns cut times; does not edit the timeline.",
    "detect_subjects": "Detect subjects in ONE selected video clip using the installed Subject Intelligence CV pack: faces (every frame), people and objects. Returns frame-indexed, confidence-scored boxes as evidence — counts, positions, timing. Use it before claims like \"the presenter is alone\" or \"no one touches the product\". Fails honestly when no clip is selected or the pack is not installed; never guess who or what is on screen.",
    "discover_caption_styles": "Browse the production caption design system before styling captions. Returns the bundled fonts (with weight ranges) and matching caption templates. Filter templates by category or search label/category/font. Use only returned font families and template ids so preview and export remain identical.",
    "discover_effects": "Browse the effect catalog. Search by name, tag or use case (\"vhs\", \"teal orange\", \"censor\"), or filter by category. Returns each effect’s id, what it looks like, and its tunable parameters WITH their real ranges and defaults. Call this before apply_effect or adjust_effect — the ids and parameter names are not guessable, and out-of-range values are rejected by the patch validator.",
    "discover_transitions": "Browse the transition catalog. Search by name, direction, feel or use case (\"left\", \"fast\", \"cinematic\", \"social media\"), or filter by category. Returns each transition’s id, what it does, its default length, and the parameters it actually reads. Call this before add_transition — the ids are not guessable, and a kind this build does not know is refused outright rather than rendering as nothing.",
    "export_video": "Render the final export video.",
    "find_similar": "Semantic similarity search over the project's media — use for \"find moments like X\" / \"shots similar to this\" where exact words may differ. Returns ranked hits like search_media; blends meaning-based and keyword matches when an embeddings model is configured, and honestly degrades to keyword-only when not (the result says which). Does not edit the timeline.",
    "generate_mask": "Generate a subject mask (unavailable — segmentation produces bitmap masks, and timeline masks steer by rectangle bounds). The measured alternative is track_subject_automatically with subject=\"silhouette\": it segments inside a drawn mask and animates that mask to follow the measured silhouette.",
    "get_clip": "Return one clip in full detail (effects, keyframes, styling) plus its trackId. The precise deep read to pair with the compact get_clips listing.",
    "get_clips": "List clips as compact rows — ids, times, source in/out, `cropped` and `graded` flags, effect/keyframe counts — optionally filtered to one trackId and/or a start/end window (timeline seconds), paginated with offset/limit (default 50, max 200). Returns { clips, total, hasMore }. `cropped` is how you check reframing coverage across a whole cut in ONE call: an uncropped clip whose source aspect differs from the sequence renders with black bars. Use this instead of get_timeline on a long-form project; use get_clip for one clip in full detail.",
    "get_frame": "LOOK at the edit: render one frame of the timeline at a given time and see it as an image. Use it to CHECK your own work visually — caption placement and legibility, framing after a punch-in or reframe, whether a title collides with the footage, whether a grade reads as intended. Prefer it over guessing from numbers whenever the question is about how something LOOKS. It renders through the same engine as the final export, so what you see is what will be delivered. One frame per call, and each costs real context — grab the few moments that actually settle the question, not a sweep of the timeline.",
    "get_mapped_transcript": "The transcript as it plays on the EDITED timeline: only words that survived the cuts, each with its sequence time, its original source time, and the clip carrying it. Also returns the RUNS — the stretches of continuous audio, with their sequence bounds and word counts. A run boundary is a break in the speech: a caption cue may span as many picture cuts as you like but must never cross a run boundary, because the words either side were never spoken in one breath. Partition the words by run bounds to segment cues. This is what captions, markers, and any \"quote the video at time T\" answer must be built from. Words in deleted footage are gone, so anything you read here is genuinely audible.",
    "get_project_state": "Return the current editable project state — settings, timeline, transcript, markers, memory — without editor-only undo history. This is the live state for the active session: read it here, not from project.fp.json on disk. The media bin comes back as a TALLY, not a listing; call list_assets for the asset ids.",
    "get_selected_range": "Return the user's selected timeline range (start/end seconds), or null when nothing is selected.",
    "get_timeline": "Return the current timeline (tracks/clips). Pass start/end (timeline seconds) to read only the clips playing in that window — on a long sequence, read sections rather than dumping every clip.",
    "get_timeline_map": "THE authoritative source↔sequence timing for every clip: assetId, source in/out, sequence in/out, speed, track — plus the sequence duration and the timeline revision. Read this whenever you need to relate a moment in the original footage to a moment on the edit. Never compute that relationship yourself from clip durations, and never reuse a mapping you read earlier: any cut, trim, move or speed change makes it wrong, and the revision tells you it did. It returns EVERY clip and takes no arguments — on a long timeline read a window with get_clips (same source in/out, filtered by track/time and paginated) instead.",
    "get_timeline_summary": "Return a compact overview of the timeline: total duration, and per track its id, type, flags, clip count, and first/last clip times — plus marker and transcript-word counts. Orient with this first on a large project; it is far cheaper than get_timeline, which dumps every clip.",
    "get_transcript": "Return the word-level transcript in SOURCE time — where each word sits in the original recording, NOT where it plays on the edited timeline. The two are the same only before any cut. To place captions, add markers, or reference a moment on the timeline, use get_mapped_transcript instead; never convert these timestamps yourself. Pass start/end (source seconds) to read only that window — on a long recording, read sections, not the whole thing.",
    "index_media": "Build (or finish) the visual index so search_visual and describe_footage can see the footage: samples frames, embeds them, and captions scenes. Call it when a visual search reports the footage is not indexed yet. By default it waits until indexing is complete (wait: false returns after starting, to continue in the background); assetId indexes just that asset, omitted indexes every video/image the project knows. Needs an embedding key configured — reports honestly when none is set. Does not edit the timeline.",
    "list_assets": "List the media-bin assets and folders. A focused, cheaper read than get_project_state when you only need the media library — optionally filter by kind (video/audio/image) and/or folderId — OMIT a filter you do not need rather than passing an empty value. Returns { assets, folders }, each asset as { id, path, kind, durationSeconds, folderId }, plus `orientation`/`aspect` for picture the engine has measured — absent means unmeasured, never square. A `letterbox` note names any landscape source in a portrait project, which is the one thing you cannot work out from the ids.",
    "list_edit_boundaries": "Every real cut in the sequence — where one clip ends and the next begins — with the two clip ids, the sequence time, and the longest transition each can carry. A transition can only go at one of these. A narrative pivot INSIDE a continuous clip is not a boundary: split the clip there first, or the transition has nothing to happen at.",
    "load_skill": "Load the full instructions of a skill from the skills manifest in your context. Call it BEFORE starting work the skill covers, then follow the returned playbook. Returns { name, description, tools, body } or the list of valid names when the skill is unknown.",
    "manage_assets": "Organize the media bin into folders. Provide an explicit semantic plan (folders + assignments) to group assets by meaning (e.g. \"B-roll\", \"Music\"), or pass strategy=\"by-kind\" to auto-group into Video/Audio/Images.",
    "map_footage": "Map the WHOLE footage with no query — the FIRST move on unfamiliar or long material before you plan any edit. Returns a time-ordered digest { chapters: [{ t0, t1, title, summary, assetId }], highlights, summary, durationSec, timeBase, unplacedAssets }, so you can see the story shape at a glance and decide where to cut, tighten, punch in, or reframe. READ timeBase before acting on a time: \"timeline\" means the times are timeline seconds and you can act on them directly; \"asset\" means they are that footage's own source seconds. Assets listed in unplacedAssets are not on the timeline at all, so their rows are source seconds whatever timeBase says — place the asset before cutting to one. Then drill into a chapter with describe_footage / search_visual (which always answer in asset seconds). Optional assetId maps just one asset; refresh recomputes past the cache. Honestly reports when the footage is not indexed yet or generative understanding is unavailable. Does not edit the timeline.",
    "map_time": "Convert one timestamp between the original footage and the edited sequence. Give { sourceTime, assetId } to ask where a moment of footage ended up — the answer is a LIST, because it may have been cut (empty) or used more than once. Give { sequenceTime } to ask what plays at a moment of the edit. Use this instead of doing the arithmetic; it accounts for trims, speed, and reuse. Called with no arguments it returns the whole timeline map.",
    "measure_color": "Measure one timeline shot through the deterministic render path before matching color. Returns a revision-bound evidence handle with RGB/luma/saturation distributions and whether another visible layer contaminated the sample. Call once for the target and once for the reference, then pass both handles to professional_color match_reference. Never invent or copy the numeric measurements.",
    "move_clip": "Move a clip to a track at a new timeline start time (duration unchanged).",
    "move_effect": "Move an effect layer to a new start time, keeping its duration. Pass toTrackId to move it onto a different effect lane (which changes the order it combines in — lower lanes apply first).",
    "move_track": "Reorder a track to a new z-order slot. toIndex 0 is the visual front (nearer the viewer); clips are untouched. Use to put an overlay above the footage it should cover, or push b-roll behind a title.",
    "professional_audio": "Mix the selected audio: level (gain, frame-based fades, mute, peak normalize), eq (shelves, peaks, and high/low-pass to clean up a recording), compress (even out a performance), automate_gain (ride the level over time), or duck one source under another. Use duck_roles with bedRole/sidechainRole for \"duck the music under the dialogue\" — it uses the roles the editor authored on the tracks. Use duck_selection when tracks carry no roles: select the bed clip last so it is primary, plus a clip on the sidechain track. An automation lane is the level over time and replaces the static gain, so author one or the other, not both. Roles and ids come from live editor state and authored labels, never from track or file names.",
    "professional_color": "Apply explicit bounded primary corrections, or match this shot from two trusted measure_color evidence handles. Uses one canonical correction node per clip and preserves separate creative LUT/look layers. Matching derives conservative exposure, contrast, saturation, temperature, and tint from revision-bound unobstructed measurements; stale, incomplete, wrong-clip, or occluded evidence fails closed. Set groupShots to grade every shot from the same camera file at once — \"match all of camera B\" — instead of one clip. Set preserveSkin on a match to hold faces where they are: the white-balance part of the match is scaled back until skin warmth stays inside tolerance, and a shot with too little skin to read says so rather than pretending nothing moved.",
    "professional_edit": "Perform a professional roll, slip, ripple trim, slide, lift, extract, insert, overwrite, replace, J-cut, L-cut, or camera switch against the live selection, playhead, and source monitor. Use switch_angle with cameraAngleId to cut to another camera in the same synced group: it cuts at the playhead, lands on the same instant through the authored sync offsets, and leaves the sound alone. Linked picture/sound edits preserve sync by default; allow_desync must be explicit. Referents, source marks, and linked edit points are resolved from authoritative editor state; ambiguous or stale targets are rejected, as are cameras with no authored sync. Supply intent and frame counts only—FramePilot compiles the primitive operations.",
    "professional_motion": "Animate the selected clip/property to a target value or continue its established trajectory using clip-frame timing and deterministic easing. The live selection/playhead resolves the target; property and canvas-cover constraints fail closed. Supply duration in frames, not raw keyframe arrays.",
    "professional_tracking_mask": "Track the existing bounded rectangle/ellipse mask on the selected shot using deterministic manual corrections. The mask supplies the region and motion; never guess coordinates. Automatic face/object/planar/segmentation tracking is unavailable until a real CV engine is installed.",
    "punch_in": "Add a zoom/punch-in (animated scale) to a clip. Times are clip-relative; the window defaults to the whole clip.",
    "read_edit_signals": "Describe what is measurably THERE across a stretch of the edit — the facts a move should be chosen from, never the move itself. Pass the signals you have already gathered (map_footage chapters/highlights, analyze_silence ranges, detect_scenes cuts); returns them in TIME order as [{ kind: highlight|chapter|silence|emphasis|scene_change, t0, t1, observation, from }] in timeline seconds, with each chapter's shape (length, highlights inside, words spoken) and each silence long enough to notice. Transcript emphasis is measured from the project for you. `from` says whether a signal was supplied by you or measured here — a chapter you did not read from the footage is still only your own claim. WHICH move each observation deserves — a punch-in, a reframe, a ramp, a cut, nothing at all — is your judgement, and this tool deliberately does not rank or recommend. Does not edit the timeline.",
    "recall_evidence": "Re-open something you already read. Every read you make this run is filed under a short handle like [ev_3] shown next to it in your action log; pass that handle to get the full result back, optionally narrowed by a word or phrase. Use this instead of re-running the read — it is free, it cannot change under you, and it returns more of the payload than the log preview shows. A query matches on any of its words, so several keywords are fine. When a result says it was truncated at N characters, call again with offset N to read on from there.",
    "remember_preference": "Remember how this editor likes their videos, so the next session starts knowing it. Use it when they state a lasting preference (\"punchier cuts than that\", \"always big yellow captions\", \"this is for founders\") — NOT for a one-off instruction about the edit in front of you, which belongs in the edit and not in memory. Keys: preferredPacing, captionStyle, brandStyle, targetAudience, plus exportPlatforms for where this project is published. Writing a key replaces what was there. Reversible like any other edit, and stored in the project file.",
    "remove_effect": "Delete an effect layer from the timeline. Reversible.",
    "remove_marker": "Remove a marker/chapter by id (schema v9). Reversible.",
    "remove_silences": "Cut the dead air out of a recording in ONE call: measures the silences in the asset that plays on the timeline and ripple-deletes them where that asset is placed, keeping keepSeconds of breath on each side so words never touch. Use this instead of analyze_silence followed by many delete_range calls. assetId names the recording (default: the asset under the first picture clip); minSilenceSeconds (default 0.8) ignores natural pauses; trackId limits the cuts to one track. Returns how many cuts and seconds were removed. Returns a reversible patch.",
    "remove_track": "Remove a track (a \"layer\") by id, including any clips on it. Reversible (undo restores the track with its clips), but prefer targeted clip edits — removing a populated track that holds prior work is rejected unless the user themselves asked for it.",
    "render_preview": "Render a fast low-res preview.",
    "resize_effect": "Change an effect layer’s in/out points — trim, extend or shorten it. Both times are absolute timeline seconds.",
    "ripple_delete": "Delete a timeline range (seconds) on one track and close the gap — later clips shift earlier. Prefer this for cutting dead air or tightening pacing.",
    "search_media": "Full-text search over the transcript, markers, and asset names — use for \"find where I said X\" instead of reading the whole transcript. Returns ranked hits { type, assetId?, markerId?, start, end, snippet, score } with times in timeline seconds (asset hits add clip placements); does not edit the timeline.",
    "search_music": "Search for background music to lay under the edit — say the mood or instrument you want (\"calm piano\", \"driving synth\"), not a song title. Two or three words: the library matches a PHRASE, so a long mood sentence silently narrows to its opening words and the rest is discarded — the result says which query actually matched, and re-running a longer one will not help. Returns candidate tracks { remoteId, title, durationSeconds, license, attributionRequired, creator }; play nothing, download nothing, spend nothing. Pass a remoteId to add_music to actually use one. Every result is cleared for monetized video; some require crediting the artist, and the result says which. A result carries NO tempo, key, energy or section structure — this library does not publish them, so a title is all you have to go on and you cannot rank candidates by BPM or by where their drop is from here. To actually know a track's rhythm, add_music it and run detect_beats on the asset; that is the only route, and undo removes the track, its layer and the file in one step if you then want a different one. Say you chose on mood and title if that is what you did. Does not edit the timeline.",
    "search_stock": "Search a stock library for a shot the footage does not contain — say the subject (\"city skyline at dusk\", \"hands typing\"), not a filename. Reach for this only when the script genuinely calls for something the user never shot: on a screen recording or a product demo, a punch-in or a reframe of their own frame is almost always the better cut. Returns candidates { remoteId, kind, title, durationSeconds, width, height, creator }; downloads nothing and spends nothing. Pass a remoteId to add_stock to use one. Leave `orientation` off and it follows the project's own frame — set it only to reframe deliberately, never to source landscape plates for a vertical cut. Does not edit the timeline.",
    "search_visual": "Search what is actually ON SCREEN across the footage — the primary way to GROUND any content-dependent edit (\"cut to the product shot\", \"where does the whiteboard appear\", \"find the b-roll of the city\"). Retrieves ranked evidence packets { assetId, t0, t1 (asset seconds), sceneId, score, caption, transcriptOverlap, sources } fusing visual-vector, caption, and transcript recall. Prefer this over guessing from dialogue: read the captions/spans and cite them. Honestly degrades (available with a reason, no packets) when the footage is not indexed or no embedding key is set. Optional k (1-50), assetIds, and timeRange narrow recall. Does not edit the timeline.",
    "session_context": "Read what's already known about this project before doing anything else: the media bin digest, what happened in the last session, the edits this user rejected (do not repeat them) and accepted, plus their cross-project working style. Use at the start of a session, or when you need to know what the user has already told us. Does not edit the timeline.",
    "set_caption_style": "Override one caption cue after applying the track-wide design. Supports the full composition surface: font/template, weight/style/scale, colors/outline, xPercent/yPercent placement, rotation, maximum width, alignment, line height, safe area, letter spacing, background/padding, shadow, highlight, animation and accent. Prefer captionStyle: { templateId } naming a template from discover_caption_styles (it lists every template with its category); any explicit field overrides the template. Load the caption-design skill for selection guidance. Unbundled fonts and unknown templates are rejected. Pass captionStyle: null to clear styling back to unstyled. Meaningful on caption clips created by add_caption_layer.",
    "set_clip_blend_mode": "Set how a clip composites over the layers beneath it (schema v8): e.g. 'screen', 'multiply', 'overlay', 'soft-light'. blendMode: null resets to 'normal'. Meaningful for overlay-track clips (light leaks, textures, glows).",
    "set_clip_crop": "Crop/reframe a clip to a rectangle of its source frame (schema v7), given as 0..1 fractions { x, y, width, height } from the top-left corner. crop: null clears the crop back to the full frame. Use it to reframe 16:9 footage into a 9:16 subject-centered crop.",
    "set_clip_speed": "Set a clip's constant playback speed (schema v6 time-remap): 2 plays it 2× faster, 0.5 at half speed. speed: null resets to 1×. The clip’s timeline length is recomputed from its (unchanged) source in/out points, so a speed-up shortens the clip and a slow-down lengthens it.",
    "set_effect_enabled": "Temporarily bypass an effect layer, or re-enable it. The layer stays on the timeline either way — use remove_effect to delete it.",
    "set_track_caption_style": "Set the complete shared caption composition for one caption track. This is the AI equivalent of the Captions panel controls: choose a discovered template/font; set font weight/style/scale, colors, outline, xPercent/yPercent placement, rotation, maximum width, alignment, line height, safe area, spacing, padding/background, shadow, animation, and accent behavior. Per-cue set_caption_style overrides still win. Pass captionStyle: null to clear the track default. Call discover_caption_styles first; unbundled fonts and unknown templates are rejected.",
    "set_track_flags": "Mute/unmute, lock/unlock, or hide/show a track (schema v4). Muting silences a track's audio in the render; hiding drops a visual track's picture; locking prevents edits. Only the provided flags change; omit a flag to leave it as-is.",
    "split_clip": "Split a clip in two at a timeline time strictly inside the clip.",
    "track_object": "Attach an object tracker to a clip. target=\"object\" tracks an arbitrary picked region (frame fractions); a track is computed by the tracking engine.",
    "track_subject_automatically": "Track the subject inside the rectangle/ellipse mask on ONE selected video clip using an installed CV pack and apply the measured motion to that mask as a reversible tracked effect. subject=\"point\"|\"region\"|\"plane\" use geometric tracking; subject=\"silhouette\" segments the person inside the mask (Subject Intelligence pack) and follows their measured outline bounds per frame. Requires the user to have drawn a rectangle or ellipse mask around the subject first — the mask supplies the region; you never supply coordinates. Fails honestly when no clip is selected, the mask is missing, no pack is installed, or the measured track is too unreliable to apply.",
    "transcribe": "Transcribe an audio or video asset with the configured speech-to-text provider. The trusted host produces word timestamps and writes them through a reversible set_transcript patch; never provide transcript words yourself.",
    "trim_clip": "Set a clip's new start/end in timeline seconds; the source in/out shifts by the same amount. Use to tighten or extend one clip's edges. It cannot change WHERE IN THE ASSET a clip reads from while keeping its timeline position and length — to do that, delete_clip it and add_clip the same span with a different sourceStart.",
    "verify_captions": "Check the caption track against the edited timeline and report what is actually wrong: cues outside the sequence, cues over deleted speech or a gap, cues out of sync with the mapped word timings, cues whose words a later edit replaced, cues bridging a break in the SPEECH (two stretches of audio never spoken in one breath), and retained speech with no caption. A cue sitting over several picture cuts is fine and is not reported — only an audio discontinuity is. Returns { ok, cueCount, issues[] }. Run this before saying captions are done: an operation returning \"applied\" is not evidence that anything is synchronized. Repair whatever it reports by re-running caption_the_edit, which re-derives every cue from the current timeline in one call — do not delete and re-add cues one at a time. It checks TIMING only — it cannot see whether a cue is legible, clipped by the frame edge, or sitting on a face.",
    "verify_transitions": "Read back every transition actually present in timeline state and check each sits at a real cut, references the correct adjacent clips, and has a duration the boundary can carry. Also reports boundaries you may have intended to treat but did not. Returns { ok, transitionCount, issues[] }. Run this before saying a transition was added; a command being accepted is not proof it is visible.",
}
