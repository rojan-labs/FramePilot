# Visual Describe — third-party licenses

Generated by `tools/generate_sbom.py` from the resolved environment, `uv.lock`
and `pack/models.lock.toml`. Do not edit by hand; regenerate after any change.

## Model weights and the llama.cpp runtime

Fetched at pack build time from a URL pinned to an immutable upstream release or
revision, and verified against these digests. They are never committed to the
repository and never enter the base installer.

| Artifact | File | License | Verified | Bytes | SHA-256 |
| --- | --- | --- | --- | --- | --- |
| SmolVLM2-2.2B-Instruct multimodal projector, f16 GGUF | `mmproj-SmolVLM2-2.2B-Instruct-f16.gguf` | Apache-2.0 | ❌ open — see notes | 872303680 | `db9a3a1648cab1ebc3af4a2b0c8145dd8faebf6f7dd7b16e7dc1842229f14ac4` |
| SmolVLM2-500M-Video-Instruct multimodal projector, f16 GGUF | `mmproj-SmolVLM2-500M-Video-Instruct-f16.gguf` | Apache-2.0 | ❌ open — see notes | 199470624 | `b5dc8ebe7cbeab66a5369693960a52515d7824f13d4063ceca78431f2a6b59b0` |
| llama.cpp multimodal CLI (llama-mtmd-cli) | `llama-mtmd-cli` | MIT | ❌ open — see notes | 85120 | `b61a6f2b996b6068f36399ac5b54503856071596aef5ff3e665d311ead7e7884` |
| libggml.0.23.0.dylib | `libggml.0.23.0.dylib` | MIT | ❌ open — see notes | 60128 | `98fa46f0bcd8c75a335f733c38cb79f911bc39412ba15ef52fee3ac051c14069` |
| libggml-base.0.23.0.dylib | `libggml-base.0.23.0.dylib` | MIT | ❌ open — see notes | 732776 | `40a133d388655a8826b298e244a083fcfc4cc219f4b51018b19e6d8ebb64d42c` |
| libggml-blas.0.23.0.dylib | `libggml-blas.0.23.0.dylib` | MIT | ❌ open — see notes | 58776 | `414856968dcddae3a67e0f9c6dc012e5c4db13320c4d7ea5924dc1ad192dcfd6` |
| libggml-cpu.0.23.0.dylib | `libggml-cpu.0.23.0.dylib` | MIT | ❌ open — see notes | 968688 | `e63a7b4f8eaa5bbd8e93e524d1a828de22a42bead57ae79cfab46e2339571fb2` |
| libggml-metal.0.23.0.dylib | `libggml-metal.0.23.0.dylib` | MIT | ❌ open — see notes | 2128584 | `c23aa0bd992066b1bc81c2f4a58798fdc6b5384c1df9dba91812db72e95a1833` |
| libggml-rpc.0.23.0.dylib | `libggml-rpc.0.23.0.dylib` | MIT | ❌ open — see notes | 191808 | `d998f743ae44021c8990a95765585d813bf161fcc8c7a527ed9f746e0f76d144` |
| libllama.0.4.0.dylib | `libllama.0.4.0.dylib` | MIT | ❌ open — see notes | 3112080 | `5279dde6e366d797f31fe699985d089ee4dbbbd6d5fd33c02defbc7ede0a34fb` |
| libllama-common.0.4.0.dylib | `libllama-common.0.4.0.dylib` | MIT | ❌ open — see notes | 7641000 | `773df6d3afacf1b93bab1a5cad0bc327f537bdee959e9906a794cb9403f0b6e9` |
| libmtmd.0.4.0.dylib | `libmtmd.0.4.0.dylib` | MIT | ❌ open — see notes | 1378752 | `6e902b5676376c466dc4a1ed66a3b2c675589b47a5292581a3bc7cec895516fa` |
| SmolVLM2-2.2B-Instruct, Q4_K_M GGUF | `SmolVLM2-2.2B-Instruct-Q4_K_M.gguf` | Apache-2.0 | ❌ open — see notes | 1112602656 | `0cf76814555b8665149075b74ab6b5c1d428ea1d3d01c1918c12012e8d7c9f58` |
| SmolVLM2-500M-Video-Instruct, Q8_0 GGUF | `SmolVLM2-500M-Video-Instruct-Q8_0.gguf` | Apache-2.0 | ❌ open — see notes | 436808704 | `6f67b8036b2469fcd71728702720c6b51aebd759b78137a8120733b4d66438bc` |

Copyright holders:

- **SmolVLM2-2.2B-Instruct multimodal projector, f16 GGUF** — Hugging Face
- **SmolVLM2-500M-Video-Instruct multimodal projector, f16 GGUF** — Hugging Face
- **llama.cpp multimodal CLI (llama-mtmd-cli)** — Georgi Gerganov and the ggml authors
- **libggml.0.23.0.dylib** — Georgi Gerganov and the ggml authors
- **libggml-base.0.23.0.dylib** — Georgi Gerganov and the ggml authors
- **libggml-blas.0.23.0.dylib** — Georgi Gerganov and the ggml authors
- **libggml-cpu.0.23.0.dylib** — Georgi Gerganov and the ggml authors
- **libggml-metal.0.23.0.dylib** — Georgi Gerganov and the ggml authors
- **libggml-rpc.0.23.0.dylib** — Georgi Gerganov and the ggml authors
- **libllama.0.4.0.dylib** — Georgi Gerganov and the ggml authors
- **libllama-common.0.4.0.dylib** — Georgi Gerganov and the ggml authors
- **libmtmd.0.4.0.dylib** — Georgi Gerganov and the ggml authors
- **SmolVLM2-2.2B-Instruct, Q4_K_M GGUF** — Hugging Face
- **SmolVLM2-500M-Video-Instruct, Q8_0 GGUF** — Hugging Face

### Licence rows not independently verified

`licenseVerified` in `pack/models.lock.toml` is `false` for these — the licence in
the table above is what the release/model card declares, not a licence file this
project has itself read inside the pinned archive or revision:

- **SmolVLM2-2.2B-Instruct multimodal projector, f16 GGUF** (`mmproj-SmolVLM2-2.2B-Instruct-f16.gguf`). The vision projector. Without it the model is text-only and this pack has nothing to describe.
- **SmolVLM2-500M-Video-Instruct multimodal projector, f16 GGUF** (`mmproj-SmolVLM2-500M-Video-Instruct-f16.gguf`). Projector for the low-RAM pair.
- **llama.cpp multimodal CLI (llama-mtmd-cli)** (`llama-mtmd-cli`). Executed as a subprocess over the user's footage, so it is a LARGER trust decision than the weights it loads and is pinned by the same rule.
- **libggml.0.23.0.dylib** (`libggml.0.23.0.dylib`). Loaded into the runtime's own process — the tensor library the engine is built on. It runs the user's footage exactly as the CLI does, so it is pinned by the same rule rather than trusted for sitting in the same tarball.
- **libggml-base.0.23.0.dylib** (`libggml-base.0.23.0.dylib`). Loaded into the runtime's own process — ggml's own base layer; every other ggml module links it. It runs the user's footage exactly as the CLI does, so it is pinned by the same rule rather than trusted for sitting in the same tarball.
- **libggml-blas.0.23.0.dylib** (`libggml-blas.0.23.0.dylib`). Loaded into the runtime's own process — the Accelerate/BLAS compute backend. It runs the user's footage exactly as the CLI does, so it is pinned by the same rule rather than trusted for sitting in the same tarball.
- **libggml-cpu.0.23.0.dylib** (`libggml-cpu.0.23.0.dylib`). Loaded into the runtime's own process — the CPU compute backend. It runs the user's footage exactly as the CLI does, so it is pinned by the same rule rather than trusted for sitting in the same tarball.
- **libggml-metal.0.23.0.dylib** (`libggml-metal.0.23.0.dylib`). Loaded into the runtime's own process — the Metal GPU backend — this is what makes the pack usable on a laptop. It runs the user's footage exactly as the CLI does, so it is pinned by the same rule rather than trusted for sitting in the same tarball.
- **libggml-rpc.0.23.0.dylib** (`libggml-rpc.0.23.0.dylib`). Loaded into the runtime's own process — ggml's RPC backend; linked by the engine whether or not it is used. It runs the user's footage exactly as the CLI does, so it is pinned by the same rule rather than trusted for sitting in the same tarball.
- **libllama.0.4.0.dylib** (`libllama.0.4.0.dylib`). Loaded into the runtime's own process — the inference engine itself. It runs the user's footage exactly as the CLI does, so it is pinned by the same rule rather than trusted for sitting in the same tarball.
- **libllama-common.0.4.0.dylib** (`libllama-common.0.4.0.dylib`). Loaded into the runtime's own process — argument parsing and sampling shared by the CLIs. It runs the user's footage exactly as the CLI does, so it is pinned by the same rule rather than trusted for sitting in the same tarball.
- **libmtmd.0.4.0.dylib** (`libmtmd.0.4.0.dylib`). Loaded into the runtime's own process — the multimodal layer — image encoding and the projector bridge. It runs the user's footage exactly as the CLI does, so it is pinned by the same rule rather than trusted for sitting in the same tarball.
- **SmolVLM2-2.2B-Instruct, Q4_K_M GGUF** (`SmolVLM2-2.2B-Instruct-Q4_K_M.gguf`). ~1.5 GiB resident. Verify the QUANTISATION's licence, not only the upstream model card.
- **SmolVLM2-500M-Video-Instruct, Q8_0 GGUF** (`SmolVLM2-500M-Video-Instruct-Q8_0.gguf`). Loaded instead of the default on machines under 12 GB. Recorded under its OWN model id, so a ledger row always names the weights that produced it.

## Python distributions

| Component | Version | License |
| --- | --- | --- |
| `numpy` | 2.5.3 | BSD-3-Clause AND 0BSD AND MIT AND Zlib AND CC0-1.0 |
| `opencv-contrib-python-headless` | 5.0.0.93 | Apache-2.0 |

## Native libraries redistributed inside the OpenCV wheel

These ship as binaries inside `cv2/` and are named by the wheel's own
`LICENSE-3RD-PARTY.txt`, which `--check` verifies still lists every one of them.

| Library | License | Platforms | Note |
| --- | --- | --- | --- |
| `FFmpeg` | LGPL-2.1-or-later | darwin-arm64, win32-x64 | Media decode used by VideoCapture. Redistributed in every opencv-python wheel. |
| `libbluray` | LGPL-2.1-or-later | darwin-arm64 | Redistributed inside the macOS opencv-python wheel. |
| `libgnutls` | LGPL-2.1-or-later | darwin-arm64 | Redistributed inside the macOS opencv-python wheel. |
| `libnettle` | LGPL-2.1-or-later | darwin-arm64 | Redistributed inside the macOS opencv-python wheel. |
| `libhogweed` | LGPL-2.1-or-later | darwin-arm64 | Redistributed inside the macOS opencv-python wheel. |
| `libintl` | LGPL-2.1-or-later | darwin-arm64 | Redistributed inside the macOS opencv-python wheel. |
| `libmp3lame` | LGPL-2.1-or-later | darwin-arm64 | Redistributed inside the macOS opencv-python wheel. |
| `libp11` | LGPL-2.1-or-later | darwin-arm64 | Redistributed inside the macOS opencv-python wheel. |
| `librtmp` | LGPL-2.1-or-later | darwin-arm64 | Redistributed inside the macOS opencv-python wheel. |
| `libsoxr` | LGPL-2.1-or-later | darwin-arm64 | Redistributed inside the macOS opencv-python wheel. |
| `libtasn1` | LGPL-2.1-or-later | darwin-arm64 | Redistributed inside the macOS opencv-python wheel. |
| `libvpx` | BSD-3-Clause | not shipped | Linux opencv-python wheels only; not shipped in the macOS/Windows pack artifacts. |

## Obligations this pack carries

- **Runtime attribution.** `llama-mtmd-cli` and its nine dylibs are llama.cpp
  (MIT); this notice must travel with the binaries, which is why it ships inside
  the pack artifact and is surfaced in the catalog record before a user approves
  the download.
- **Model attribution.** Every GGUF weight above is Apache-2.0 (SmolVLM2). The
  quantisation and mmproj export licences are not yet independently verified
  beyond the upstream model card (see `licenseVerified` in
  `pack/models.lock.toml`) — a re-quantised or re-exported artifact could in
  principle carry different terms, which is exactly what that flag tracks.
- **No copyleft weight or binary may ship.** `tools/generate_sbom.py --check`
  fails if any pinned artifact ever carries a licence outside
  MIT/Apache-2.0/BSD-3-Clause. Qwen2.5-VL (research-only) and Gemma 3 (Gemma
  terms) were excluded on this ground and must not be reopened as a default.
- **LGPL-2.1-or-later components are redistributed.** FFmpeg (all platforms) and the
  macOS support libraries above are dynamically linked binaries inside the OpenCV wheel.
  Shipping them obliges FramePilot to distribute this notice, offer the corresponding
  source for those components, and keep them replaceable — they must stay separate
  dynamically linked binaries inside the pack artifact, never statically folded in.
  FramePilot's own code and the base application are unaffected: the pack is an isolated
  process, not a link-time dependency of the editor.
- **OpenCV itself is Apache-2.0** and NumPy is BSD-3-Clause-family, both permissive.

The catalog record for this pack must surface the LGPL obligation, a source-offer URL,
and the model/runtime provenance above before a user approves the download.
