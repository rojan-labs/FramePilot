# Visual Embed — third-party licenses

Generated by `tools/generate_sbom.py` from the resolved environment, `uv.lock`
and `pack/models.lock.toml`. Do not edit by hand; regenerate after any change.

## Model weights

Downloaded at pack build time from a URL pinned to an immutable upstream revision
and verified against these digests. They are never committed to the repository and
never enter the base installer.

| Model | File | License | Verified | Bytes | SHA-256 |
| --- | --- | --- | --- | --- | --- |
| YuNet | `face_detection_yunet_2023mar.onnx` | MIT | ✅ | 232589 | `8f2383e4dd3cfbb4553ea8718107fc0423210dc964f9f4280604804ed2552fa4` |
| SFace | `face_recognition_sface_2021dec.onnx` | Apache-2.0 | ✅ | 38696353 | `0ba9fbfa01b5270c96627c4ef784da859931e02f04419c829e83484087c34e79` |
| SigLIP 2 base patch16-224 (ONNX vision tower) | `siglip2_base_patch16_224_vision.onnx` | Apache-2.0 | ❌ open — see notes | 371807752 | `c0573e3f4140c3a7c4e9cc5912bd6b26a033b46a6a8e8af26cbea262b163bcad` |
| SigLIP 2 base patch16-224 (ONNX text tower) | `siglip2_base_patch16_224_text.onnx` | Apache-2.0 | ❌ open — see notes | 1129469657 | `baf12d941beabafafb14f7b4adb38dc15be18681b964a84410ec53d9d65e6293` |
| SigLIP 2 tokenizer | `siglip2_tokenizer.json` | Apache-2.0 | ❌ open — see notes | 34363039 | `cb9140fae3ac5122c972d37adf83e1248471a38147ad76f8215c8872c6fd8322` |

Copyright holders:

- **YuNet** — Shiqi Yu and contributors; OpenCV Zoo
- **SFace** — Zhong Yaoyao and contributors; OpenCV Zoo
- **SigLIP 2 base patch16-224 (ONNX vision tower)** — Google LLC
- **SigLIP 2 base patch16-224 (ONNX text tower)** — Google LLC
- **SigLIP 2 tokenizer** — Google LLC

### Licence rows not independently verified

`licenseVerified` in `pack/models.lock.toml` is `false` for these — the licence in
the table above is what the model card or an inherited `base_model` declares, not a
licence file this project has itself read at the pinned revision:

- **SigLIP 2 base patch16-224 (ONNX vision tower)** (`siglip2_base_patch16_224_vision.onnx`). The fp32 export, NOT the fp16 one: onnx_backend.py feeds float32 arrays (rescale to [0,1], then mean/std 0.5), and an fp16 graph rejects them. ~355 MiB. Preprocessing constants are transcribed in onnx_backend.py and must be confirmed against the exported config.
- **SigLIP 2 base patch16-224 (ONNX text tower)** (`siglip2_base_patch16_224_text.onnx`). Embeds the prompt bank once per bank version, and every search_visual/find_similar query.
- **SigLIP 2 tokenizer** (`siglip2_tokenizer.json`). Gemma-family SentencePiece vocabulary in tokenizers' JSON form; context length 64.

## Python distributions

| Component | Version | License |
| --- | --- | --- |
| `PyYAML` | 6.0.3 | MIT |
| `anyio` | 4.15.1 | MIT |
| `certifi` | 2026.7.22 | MPL-2.0 |
| `click` | 8.5.0 | BSD-3-Clause |
| `filelock` | 3.32.5 | MIT |
| `flatbuffers` | 25.12.19 | Apache-2.0 |
| `fsspec` | 2026.7.0 | BSD-3-Clause |
| `h11` | 0.16.0 | MIT |
| `hf-xet` | 1.6.0 | Apache-2.0 |
| `httpcore` | 1.0.9 | BSD-3-Clause |
| `httpx` | 0.28.1 | BSD-3-Clause |
| `huggingface_hub` | 1.30.0 | Apache-2.0 |
| `idna` | 3.19 | BSD-3-Clause |
| `numpy` | 2.5.3 | BSD-3-Clause AND 0BSD AND MIT AND Zlib AND CC0-1.0 |
| `onnxruntime` | 1.29.0 | MIT |
| `opencv-contrib-python-headless` | 5.0.0.93 | Apache-2.0 |
| `packaging` | 26.3 | Apache-2.0 OR BSD-2-Clause |
| `protobuf` | 7.36.1 | 3-Clause BSD License |
| `tokenizers` | 0.23.2 | Apache-2.0 |
| `tqdm` | 4.70.0 | MPL-2.0 AND MIT |
| `typing_extensions` | 4.16.0 | PSF-2.0 |

## Native libraries redistributed inside the OpenCV wheel

These ship as binaries inside `cv2/` and are named by the wheel's own
`LICENSE-3RD-PARTY.txt`, which `--check` verifies still lists every one of them.

| Library | License | Platforms | Note |
| --- | --- | --- | --- |
| `FFmpeg` | LGPL-2.1-or-later | darwin-arm64, win32-x64 | Media decode used by VideoCapture. Redistributed in every opencv-python wheel. |
| `libbluray` | LGPL-2.1-or-later | darwin-arm64 | Redistributed inside the macOS opencv-python wheel. |
| `libgnutls` | LGPL-2.1-or-later | darwin-arm64 | Redistributed inside the macOS opencv-python wheel. |
| `libnettle` | LGPL-2.1-or-later | darwin-arm64 | Redistributed inside the macOS opencv-python wheel. |
| `libhogweed` | LGPL-2.1-or-later | darwin-arm64 | Redistributed inside the macOS opencv-python wheel. |
| `libintl` | LGPL-2.1-or-later | darwin-arm64 | Redistributed inside the macOS opencv-python wheel. |
| `libmp3lame` | LGPL-2.1-or-later | darwin-arm64 | Redistributed inside the macOS opencv-python wheel. |
| `libp11` | LGPL-2.1-or-later | darwin-arm64 | Redistributed inside the macOS opencv-python wheel. |
| `librtmp` | LGPL-2.1-or-later | darwin-arm64 | Redistributed inside the macOS opencv-python wheel. |
| `libsoxr` | LGPL-2.1-or-later | darwin-arm64 | Redistributed inside the macOS opencv-python wheel. |
| `libtasn1` | LGPL-2.1-or-later | darwin-arm64 | Redistributed inside the macOS opencv-python wheel. |
| `libvpx` | BSD-3-Clause | not shipped | Linux opencv-python wheels only; not shipped in the macOS/Windows pack artifacts. |

## Obligations this pack carries

- **Model attribution.** Every weight above is MIT or Apache-2.0. Both require this
  notice to travel with the binaries, which is why it ships inside the pack artifact
  and is surfaced in the catalog record before a user approves the download. The
  SigLIP 2 ONNX export's own licence is not yet independently verified beyond its
  inherited `base_model` declaration (see `pack/models.lock.toml`); the face pair's
  licence IS independently verified against the OpenCV Zoo `LICENSE` files.
- **No copyleft weight may ship.** `tools/generate_sbom.py --check` fails if any
  pinned model ever carries a licence outside MIT/Apache-2.0/BSD-3-Clause.
- **LGPL-2.1-or-later components are redistributed.** FFmpeg (all platforms) and the
  macOS support libraries above are dynamically linked binaries inside the OpenCV wheel.
  Shipping them obliges FramePilot to distribute this notice, offer the corresponding
  source for those components, and keep them replaceable — they must stay separate
  dynamically linked binaries inside the pack artifact, never statically folded in.
  FramePilot's own code and the base application are unaffected: the pack is an isolated
  process, not a link-time dependency of the editor.
- **OpenCV itself is Apache-2.0** and NumPy is BSD-3-Clause-family, both permissive.

The catalog record for this pack must surface the LGPL obligation, a source-offer URL,
and the model provenance above before a user approves the download.
